# Frontend environment variables (baked into bundle at build time by CRA)
# Copy to .env before running npm start.

# URL of the backend API — must be reachable from the user's browser.
# For Docker: http://localhost:3001
# For cloud:  https://api.yourdomain.com
REACT_APP_API_URL=http://localhost:3001
